#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_sequelize-cli@4.1.1@sequelize-cli/lib/sequelize" "$@"
  ret=$?
else 
  node  "$basedir/../_sequelize-cli@4.1.1@sequelize-cli/lib/sequelize" "$@"
  ret=$?
fi
exit $ret
